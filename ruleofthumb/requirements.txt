# Development / runtime environment for the ruleofthumb package.
# Modern-stack pins as tested on this branch (install from this directory:
#   pip install -r requirements.txt && pip install -e ".[dev]")
#
# NOTE: inputs must be rectangular batches; see ToDo.md for the
# mixed-length limitation preserved from the original research code.

# runtime (single install — everything ships with the package)
numpy==2.2.6
torch==2.13.0
torchvision==0.28.0
transformers==5.15.1
matplotlib==3.10.9
seaborn==0.13.2
wordcloud==1.9.6
pillow==12.3.0

# dev tooling (integration tests only — never imported by the package)
scikit-learn==1.7.2
pytest==9.1.1
ruff==0.16.4
build==1.5.0
nbclient==0.11.0
jupyter==1.1.1
